#include <bits/stdc++.h>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	int t;
	cin >> t;
	while(t--)
	{
	    int xa, xb, Xa, Xb, res=0;
	    cin >> xa >> xb >> Xa >> Xb;
	    res = (Xa/xa) + (Xb/xb);
	    cout << res << "\n";
	}
	return 0;
}
